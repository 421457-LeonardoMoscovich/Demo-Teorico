server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;

    # El front es una SPA: cualquier ruta que no sea un archivo cae en index.html,
    # o recargar /alumno/responder da 404.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
